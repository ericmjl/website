[model]
name = Project
label = {{ this.title }}

[fields.title]
label = Title
type = string
size = large

[fields.sort_key]
label = Order
type = sort_key

[fields.body]
label = Body
type = flow
flow_blocks = youtube, description, slides, github, resource, notebooks
